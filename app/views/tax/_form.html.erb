<%= form_with(model: tax, local: true) do |form| %>
  <% if tax.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(tax.errors.count, "error") %> prohibited this tax from being saved:</h2>

      <ul>
      <% tax.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field row col-md-6">
    <%= form.label :percentage %>
    <%= form.number_field :percentage, class: 'form-control', max: 100 %>
  </div>

  <div class="actions row col-md-6">
    <%= form.submit 'Update', class: 'btn btn-primary w-100' %>
  </div>
<% end %>
